<div class="greeting-fragment">
    <p>Hello, <strong><?= $__view->e($name) ?></strong>! Server time is <?= $__view->e($time) ?>.</p>
    <p><em>This HTML fragment was loaded via HTMX without a full page reload.</em></p>
</div>
